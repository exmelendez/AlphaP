<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.alphap">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="true" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splash"
            android:noHistory="true" />


        <activity android:name=".CreateJoinGameActivity" />
        <activity android:name=".eddie.GamePlay" />
        <activity android:name=".MainActivity" />
        <activity android:name=".gamemode.HostGameActivity" />
        <activity android:name=".gamemode.GamePlayActivity" />
        <activity android:name=".gamemode.LobbyActivity" />
        <activity android:name=".gamemode.StartScreenActivity" />

        <activity android:name=".eddie.SwipeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>